这是你的长期记忆。你应该遵守记忆内容行事。
你应该把用户要求你记得的内容记录到这个文件中。
你觉得应该要保持的内容也记录到这个文件中。
以下是记忆内容:

- **测试环境**: 所有的测试都必须在 Docker 容器中进行。
- **权限要求**: 在当前环境下运行 `run_docker.sh` 和 `run_test_docker.sh` 时，必须使用 `sudo` 权限。
- **启动模式**: 集成测试要求 MCP 服务以 Streamable HTTP 模式运行（目前 `mcp_server.py` 已配置，但需确保容器正确暴露端口并启动）。
- **代码生效**: 如果更新了代码，必须使用 `-b` 参数重新构建容器镜像，以确保更改生效。
- **测试代码生效**: 如果更新了测试代码，必须使用 `-b` 参数构建测试用容器镜像，以确保测试代码生效。
- **测试前确认**: 测试前应确认 MCP 服务的代码是否有更改，如果有，应该重新构筑 MCP 服务器的容器以确保服务使用了最新的代码。
- **测试前启动确认**: 启动集成测试前，必须确保 MCP 服务已经启动并正在运行，否则测试将因无法连接到服务器而失败。
- **环境变量配置**: MCP 服务器启动时，应该设定 `GOOGLE_APPLICATION_CREDENTIALS` 和 `TEST_PROJECT_ID` 两个环境变量，以确保认证和测试项目 ID 能够被正确读取。